import { PokemonSearchForm } from './pokemon-search-form';

describe('PokemonSearchForm', () => {
  it('should create an instance', () => {
    expect(new PokemonSearchForm()).toBeTruthy();
  });
});
